# Esempio di Vagrantfile
Vagrant.configure("2") do |config|
  config.vm.box = "windows-custom" 

  # Mappa una cartella del tuo progetto Linux nella VM Windows
  config.vm.synced_folder ".", "/vagrant" 
end

